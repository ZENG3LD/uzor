//! Event processor + input state — convenience re-exports for consumers of
//! `uzor-window-hub` so they don't need to depend on `uzor` directly for the
//! core input types.

pub use uzor::input::EventProcessor;
pub use uzor::input::InputState;
