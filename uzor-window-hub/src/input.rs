//! Cross-platform raw input normalization (mouse / keyboard / touch).
//!
//! Empty scaffold. Mouse / key normalization already happens in
//! `uzor::input`; this module reserves the spot for cross-platform
//! helpers (touch → pointer collapse, IME quirks, modifier reconciliation).
