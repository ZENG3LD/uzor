[package]
name = "uzor-viz"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Data-visualization engine core for uzor — scales, plot-area coordinates, stateless mark/guide draw functions, and composed V1 figures (bar/curve/histogram)."
publish = false

[dependencies]
uzor = { workspace = true }

[dev-dependencies]
uzor-export = { path = "../uzor-export" }
png = "0.17"
