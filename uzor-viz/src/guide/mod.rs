//! Axis + grid guides — read [`crate::scale::Scale::ticks`] and
//! [`crate::theme::VizTheme`], draw through the same
//! [`crate::coord::PlotArea`] transform every mark uses.

pub mod axis;
pub mod grid;
