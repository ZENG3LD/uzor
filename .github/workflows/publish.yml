name: Publish to crates.io

on:
  push:
    tags:
      - "v*"

jobs:
  publish:
    name: Publish crates in dependency order
    runs-on: ubuntu-latest
    environment: crates-io

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust stable toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry/index
            ~/.cargo/registry/cache
            ~/.cargo/git/db
          key: ${{ runner.os }}-cargo-publish-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-publish-

      # -----------------------------------------------------------------------
      # Tier 1 — leaf crates (no internal uzor deps)
      # -----------------------------------------------------------------------

      - name: "[Tier 1] Publish uzor-render"
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          for i in 1 2 3; do
            OUT=$(cargo publish --package uzor-render --allow-dirty 2>&1); CODE=$?
            echo "$OUT"
            [ $CODE -eq 0 ] && break
            echo "$OUT" | grep -q "already uploaded" && { echo "Already published, skipping."; break; }
            echo "Attempt $i failed (exit $CODE), waiting 120s..."
            [ $i -lt 3 ] && sleep 120
          done
          echo "$OUT" | grep -q "already uploaded" && exit 0
          exit $CODE

      - name: "[Tier 1] Wait between publishes"
        run: sleep 30

      - name: "[Tier 1] Publish uzor-animation"
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          for i in 1 2 3; do
            OUT=$(cargo publish --package uzor-animation --allow-dirty 2>&1); CODE=$?
            echo "$OUT"
            [ $CODE -eq 0 ] && break
            echo "$OUT" | grep -q "already uploaded" && { echo "Already published, skipping."; break; }
            echo "Attempt $i failed (exit $CODE), waiting 120s..."
            [ $i -lt 3 ] && sleep 120
          done
          echo "$OUT" | grep -q "already uploaded" && exit 0
          exit $CODE

      - name: "Wait for crates.io index to update after Tier 1"
        run: sleep 120

      # -----------------------------------------------------------------------
      # Tier 2 — depends on Tier 1
      # -----------------------------------------------------------------------

      - name: "[Tier 2] Publish uzor-core"
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          for i in 1 2 3; do
            OUT=$(cargo publish --package uzor-core --allow-dirty 2>&1); CODE=$?
            echo "$OUT"
            [ $CODE -eq 0 ] && break
            echo "$OUT" | grep -q "already uploaded" && { echo "Already published, skipping."; break; }
            echo "Attempt $i failed (exit $CODE), waiting 120s..."
            [ $i -lt 3 ] && sleep 120
          done
          echo "$OUT" | grep -q "already uploaded" && exit 0
          exit $CODE

      - name: "[Tier 2] Wait between publishes"
        run: sleep 30

      - name: "[Tier 2] Publish uzor-backend-vello-common"
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          for i in 1 2 3; do
            OUT=$(cargo publish --package uzor-backend-vello-common --allow-dirty 2>&1); CODE=$?
            echo "$OUT"
            [ $CODE -eq 0 ] && break
            echo "$OUT" | grep -q "already uploaded" && { echo "Already published, skipping."; break; }
            echo "Attempt $i failed (exit $CODE), waiting 120s..."
            [ $i -lt 3 ] && sleep 120
          done
          echo "$OUT" | grep -q "already uploaded" && exit 0
          exit $CODE

      - name: "[Tier 2] Wait between publishes"
        run: sleep 30

      - name: "[Tier 2] Publish uzor-backend-vello-gpu"
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          for i in 1 2 3; do
            OUT=$(cargo publish --package uzor-backend-vello-gpu --allow-dirty 2>&1); CODE=$?
            echo "$OUT"
            [ $CODE -eq 0 ] && break
            echo "$OUT" | grep -q "already uploaded" && { echo "Already published, skipping."; break; }
            echo "Attempt $i failed (exit $CODE), waiting 120s..."
            [ $i -lt 3 ] && sleep 120
          done
          echo "$OUT" | grep -q "already uploaded" && exit 0
          exit $CODE

      - name: "[Tier 2] Wait between publishes"
        run: sleep 30

      - name: "[Tier 2] Publish uzor-backend-vello-cpu"
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          for i in 1 2 3; do
            OUT=$(cargo publish --package uzor-backend-vello-cpu --allow-dirty 2>&1); CODE=$?
            echo "$OUT"
            [ $CODE -eq 0 ] && break
            echo "$OUT" | grep -q "already uploaded" && { echo "Already published, skipping."; break; }
            echo "Attempt $i failed (exit $CODE), waiting 120s..."
            [ $i -lt 3 ] && sleep 120
          done
          echo "$OUT" | grep -q "already uploaded" && exit 0
          exit $CODE

      - name: "[Tier 2] Wait between publishes"
        run: sleep 30

      - name: "[Tier 2] Publish uzor-backend-vello-hybrid"
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          for i in 1 2 3; do
            OUT=$(cargo publish --package uzor-backend-vello-hybrid --allow-dirty 2>&1); CODE=$?
            echo "$OUT"
            [ $CODE -eq 0 ] && break
            echo "$OUT" | grep -q "already uploaded" && { echo "Already published, skipping."; break; }
            echo "Attempt $i failed (exit $CODE), waiting 120s..."
            [ $i -lt 3 ] && sleep 120
          done
          echo "$OUT" | grep -q "already uploaded" && exit 0
          exit $CODE

      - name: "[Tier 2] Wait between publishes"
        run: sleep 30

      - name: "[Tier 2] Publish uzor-backend-tiny-skia"
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          for i in 1 2 3; do
            OUT=$(cargo publish --package uzor-backend-tiny-skia --allow-dirty 2>&1); CODE=$?
            echo "$OUT"
            [ $CODE -eq 0 ] && break
            echo "$OUT" | grep -q "already uploaded" && { echo "Already published, skipping."; break; }
            echo "Attempt $i failed (exit $CODE), waiting 120s..."
            [ $i -lt 3 ] && sleep 120
          done
          echo "$OUT" | grep -q "already uploaded" && exit 0
          exit $CODE

      - name: "[Tier 2] Wait between publishes"
        run: sleep 30

      - name: "[Tier 2] Publish uzor-backend-canvas2d"
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          for i in 1 2 3; do
            OUT=$(cargo publish --package uzor-backend-canvas2d --allow-dirty 2>&1); CODE=$?
            echo "$OUT"
            [ $CODE -eq 0 ] && break
            echo "$OUT" | grep -q "already uploaded" && { echo "Already published, skipping."; break; }
            echo "Attempt $i failed (exit $CODE), waiting 120s..."
            [ $i -lt 3 ] && sleep 120
          done
          echo "$OUT" | grep -q "already uploaded" && exit 0
          exit $CODE

      - name: "Wait for crates.io index to update after Tier 2"
        run: sleep 120

      # -----------------------------------------------------------------------
      # Tier 3 — depends on Tier 2
      # -----------------------------------------------------------------------

      - name: "[Tier 3] Publish uzor-layout"
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          for i in 1 2 3; do
            OUT=$(cargo publish --package uzor-layout --allow-dirty 2>&1); CODE=$?
            echo "$OUT"
            [ $CODE -eq 0 ] && break
            echo "$OUT" | grep -q "already uploaded" && { echo "Already published, skipping."; break; }
            echo "Attempt $i failed (exit $CODE), waiting 120s..."
            [ $i -lt 3 ] && sleep 120
          done
          echo "$OUT" | grep -q "already uploaded" && exit 0
          exit $CODE

      - name: "[Tier 3] Wait between publishes"
        run: sleep 30

      - name: "[Tier 3] Publish uzor-desktop"
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          for i in 1 2 3; do
            OUT=$(cargo publish --package uzor-desktop --allow-dirty 2>&1); CODE=$?
            echo "$OUT"
            [ $CODE -eq 0 ] && break
            echo "$OUT" | grep -q "already uploaded" && { echo "Already published, skipping."; break; }
            echo "Attempt $i failed (exit $CODE), waiting 120s..."
            [ $i -lt 3 ] && sleep 120
          done
          echo "$OUT" | grep -q "already uploaded" && exit 0
          exit $CODE

      - name: "[Tier 3] Wait between publishes"
        run: sleep 30

      - name: "[Tier 3] Publish uzor-web"
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          for i in 1 2 3; do
            OUT=$(cargo publish --package uzor-web --allow-dirty 2>&1); CODE=$?
            echo "$OUT"
            [ $CODE -eq 0 ] && break
            echo "$OUT" | grep -q "already uploaded" && { echo "Already published, skipping."; break; }
            echo "Attempt $i failed (exit $CODE), waiting 120s..."
            [ $i -lt 3 ] && sleep 120
          done
          echo "$OUT" | grep -q "already uploaded" && exit 0
          exit $CODE

      - name: "[Tier 3] Wait between publishes"
        run: sleep 30

      - name: "[Tier 3] Publish uzor-mobile"
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          for i in 1 2 3; do
            OUT=$(cargo publish --package uzor-mobile --allow-dirty 2>&1); CODE=$?
            echo "$OUT"
            [ $CODE -eq 0 ] && break
            echo "$OUT" | grep -q "already uploaded" && { echo "Already published, skipping."; break; }
            echo "Attempt $i failed (exit $CODE), waiting 120s..."
            [ $i -lt 3 ] && sleep 120
          done
          echo "$OUT" | grep -q "already uploaded" && exit 0
          exit $CODE

      - name: "[Tier 3] Wait between publishes"
        run: sleep 30

      - name: "[Tier 3] Publish uzor-tui"
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          for i in 1 2 3; do
            OUT=$(cargo publish --package uzor-tui --allow-dirty 2>&1); CODE=$?
            echo "$OUT"
            [ $CODE -eq 0 ] && break
            echo "$OUT" | grep -q "already uploaded" && { echo "Already published, skipping."; break; }
            echo "Attempt $i failed (exit $CODE), waiting 120s..."
            [ $i -lt 3 ] && sleep 120
          done
          echo "$OUT" | grep -q "already uploaded" && exit 0
          exit $CODE

      - name: "[Tier 3] Wait between publishes"
        run: sleep 30

      - name: "[Tier 3] Publish uzor-macos"
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          for i in 1 2 3; do
            OUT=$(cargo publish --package uzor-macos --allow-dirty 2>&1); CODE=$?
            echo "$OUT"
            [ $CODE -eq 0 ] && break
            echo "$OUT" | grep -q "already uploaded" && { echo "Already published, skipping."; break; }
            echo "Attempt $i failed (exit $CODE), waiting 120s..."
            [ $i -lt 3 ] && sleep 120
          done
          echo "$OUT" | grep -q "already uploaded" && exit 0
          exit $CODE

      - name: "[Tier 3] Wait between publishes"
        run: sleep 30

      - name: "[Tier 3] Publish uzor-panels"
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          for i in 1 2 3; do
            OUT=$(cargo publish --package uzor-panels --allow-dirty 2>&1); CODE=$?
            echo "$OUT"
            [ $CODE -eq 0 ] && break
            echo "$OUT" | grep -q "already uploaded" && { echo "Already published, skipping."; break; }
            echo "Attempt $i failed (exit $CODE), waiting 120s..."
            [ $i -lt 3 ] && sleep 120
          done
          echo "$OUT" | grep -q "already uploaded" && exit 0
          exit $CODE

      - name: "[Tier 3] Wait between publishes"
        run: sleep 30

      - name: "[Tier 3] Publish uzor-numbers"
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          for i in 1 2 3; do
            OUT=$(cargo publish --package uzor-numbers --allow-dirty 2>&1); CODE=$?
            echo "$OUT"
            [ $CODE -eq 0 ] && break
            echo "$OUT" | grep -q "already uploaded" && { echo "Already published, skipping."; break; }
            echo "Attempt $i failed (exit $CODE), waiting 120s..."
            [ $i -lt 3 ] && sleep 120
          done
          echo "$OUT" | grep -q "already uploaded" && exit 0
          exit $CODE

      - name: "[Tier 3] Wait between publishes"
        run: sleep 30

      - name: "[Tier 3] Publish uzor-text-fx"
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          for i in 1 2 3; do
            OUT=$(cargo publish --package uzor-text-fx --allow-dirty 2>&1); CODE=$?
            echo "$OUT"
            [ $CODE -eq 0 ] && break
            echo "$OUT" | grep -q "already uploaded" && { echo "Already published, skipping."; break; }
            echo "Attempt $i failed (exit $CODE), waiting 120s..."
            [ $i -lt 3 ] && sleep 120
          done
          echo "$OUT" | grep -q "already uploaded" && exit 0
          exit $CODE

      - name: "[Tier 3] Wait between publishes"
        run: sleep 30

      - name: "[Tier 3] Publish uzor-cursor"
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          for i in 1 2 3; do
            OUT=$(cargo publish --package uzor-cursor --allow-dirty 2>&1); CODE=$?
            echo "$OUT"
            [ $CODE -eq 0 ] && break
            echo "$OUT" | grep -q "already uploaded" && { echo "Already published, skipping."; break; }
            echo "Attempt $i failed (exit $CODE), waiting 120s..."
            [ $i -lt 3 ] && sleep 120
          done
          echo "$OUT" | grep -q "already uploaded" && exit 0
          exit $CODE

      - name: "[Tier 3] Wait between publishes"
        run: sleep 30

      - name: "[Tier 3] Publish uzor-scroll-fx"
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          for i in 1 2 3; do
            OUT=$(cargo publish --package uzor-scroll-fx --allow-dirty 2>&1); CODE=$?
            echo "$OUT"
            [ $CODE -eq 0 ] && break
            echo "$OUT" | grep -q "already uploaded" && { echo "Already published, skipping."; break; }
            echo "Attempt $i failed (exit $CODE), waiting 120s..."
            [ $i -lt 3 ] && sleep 120
          done
          echo "$OUT" | grep -q "already uploaded" && exit 0
          exit $CODE

      - name: "[Tier 3] Wait between publishes"
        run: sleep 30

      - name: "[Tier 3] Publish uzor-interactive"
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          for i in 1 2 3; do
            OUT=$(cargo publish --package uzor-interactive --allow-dirty 2>&1); CODE=$?
            echo "$OUT"
            [ $CODE -eq 0 ] && break
            echo "$OUT" | grep -q "already uploaded" && { echo "Already published, skipping."; break; }
            echo "Attempt $i failed (exit $CODE), waiting 120s..."
            [ $i -lt 3 ] && sleep 120
          done
          echo "$OUT" | grep -q "already uploaded" && exit 0
          exit $CODE

      - name: "[Tier 3] Wait between publishes"
        run: sleep 30

      - name: "[Tier 3] Publish uzor-panel-api"
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          for i in 1 2 3; do
            OUT=$(cargo publish --package uzor-panel-api --allow-dirty 2>&1); CODE=$?
            echo "$OUT"
            [ $CODE -eq 0 ] && break
            echo "$OUT" | grep -q "already uploaded" && { echo "Already published, skipping."; break; }
            echo "Attempt $i failed (exit $CODE), waiting 120s..."
            [ $i -lt 3 ] && sleep 120
          done
          echo "$OUT" | grep -q "already uploaded" && exit 0
          exit $CODE

      - name: "Wait for crates.io index to update after Tier 3"
        run: sleep 120

      # -----------------------------------------------------------------------
      # Tier 4 — facade crate (depends on everything)
      # -----------------------------------------------------------------------

      - name: "[Tier 4] Publish uzor (facade)"
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
        run: |
          for i in 1 2 3; do
            OUT=$(cargo publish --package uzor --allow-dirty 2>&1); CODE=$?
            echo "$OUT"
            [ $CODE -eq 0 ] && break
            echo "$OUT" | grep -q "already uploaded" && { echo "Already published, skipping."; break; }
            echo "Attempt $i failed (exit $CODE), waiting 120s..."
            [ $i -lt 3 ] && sleep 120
          done
          echo "$OUT" | grep -q "already uploaded" && exit 0
          exit $CODE
