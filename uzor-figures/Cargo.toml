[package]
name = "uzor-figures"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Data-figure engine for uzor — scales, plot-area coordinates, stateless mark/guide draw functions, and composed figures (bar/curve/histogram) over the uzor render stack."

[dependencies]
uzor = { workspace = true }

[dev-dependencies]
uzor-export = { workspace = true }
uzor-proof-harness = { workspace = true }
png = "0.17"
